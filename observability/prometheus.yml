global:
  scrape_interval: 15s

scrape_configs:
  - job_name: petclinic
    metrics_path: /petclinic/actuator/prometheus
    static_configs:
      - targets:
          - 192.168.29.248:9966  # Host LAN IP — reachable from inside Docker Compose network on Rancher Desktop
